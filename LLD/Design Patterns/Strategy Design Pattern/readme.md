### Concepts
```
While we have a base (parent class) which derieves some general features, and then the inherited child classes adds some add on and special features on top of the general ones, then multiple child classes can have repeatition or same add on features, so strategy pattern talks about handling these.
```